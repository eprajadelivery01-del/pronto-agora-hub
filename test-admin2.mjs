import { createClient } from '@supabase/supabase-js'; import dotenv from 'dotenv'; dotenv.config(); const supabase = createClient(process.env.VITE_SUPABASE_URL, process.env.VITE_SUPABASE_ANON_KEY); async function test() { const {data:r} = await supabase.from('user_roles').select('*').eq('role', 'admin'); const {data:p} = await supabase.from('profiles').select('*').eq('role', 'admin'); console.log('user_roles:', r); console.log('profiles:', p); } test();  
